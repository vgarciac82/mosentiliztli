# UX Flows

Convención de nombres: `UX-<id>-<slug>.md` (ej. `UX-01-payroll-adjustment.md`).

Generar el contenido con la plantilla `docs/templates/UX-Flow-template.md` cuando la tabla de decisión del PRD lo requiera (p. ej. más de dos ramas de error o estados no explícitos en el AC).
